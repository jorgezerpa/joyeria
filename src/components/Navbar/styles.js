import { makeStyles } from '@mui/styles';

export default makeStyles(theme=>({
    appBar:{
        border: 'none',
        boxShadow: 'none',
    },
    toolbar:{
        border: 'none',
        boxShadow: 'none'
    },
    toolbarItem:{
        color: '#fff',
        textAlign: 'center',
        display: 'block',
        textDecoration: 'none',
    },
}))
